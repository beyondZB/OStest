这个笔记主要自己看，所以主要写些遇到的问题。
书本现在主要参考《自己动手写操作系统》

1.发现nasm boot.asm -o boot.img做了软盘镜像，也可以用。
而书上制作镜像的方法为：
					1.nasm boot.asm -o boot.bin;
					2.用虚拟机建一个空的软盘镜像;
					3.用作者提供的工具将boot.bin写入软盘镜像；
那书上这样做有什么意义呢？
书上制作镜像的方法能制作1.44m的完整镜像。
虽然512字节之后没有内容，但是凑齐1.44m。据说刻录软盘的时候一定要用1.44m的完整镜像。


2.从网上看来别人用masm写的bootsector，说是出来的文件有PE头(应该是要在windows环境下运行带的吧)，要把PE头去掉才能用来做镜像，nasm汇编出来的文件没有什么多余的东西吗?
pe头，windows可执行文件头
linux的可执行文件头为elf，nasm汇编默认不加头
