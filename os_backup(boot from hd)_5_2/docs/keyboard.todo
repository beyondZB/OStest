键盘中断没有处理pause键(6位扫描码)
笔记本上测出来的prntscr(makecode:0xE0, 0xB7  breakcode:0xE0, 0xAA ,makecode和breakcode并不是一般的相差0x80的关系)
这两个键暂时没有处理

另外不知道是不是只有我笔记本的有如下特殊情况:
1:无论按什么键,开始的两次按键都会在前面多一个num的扫描码
2:开始的两次按cpslock键,每次都会有双份的扫描码过来
